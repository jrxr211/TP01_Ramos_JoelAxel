void setup() {
  
  int A = 4;
  int B = 5;
  int C = 1;
  
  float resultadoA = (B * A) - pow(B, 2) / (4 * C);
    println("La expresión a) es: " + resultadoA);
  
  float resultadoB = (A * B) / pow(3, 2);
  println("La expresión b) es: " + resultadoB);
  
  int resultadoC = (((B + C) / 2 * A + 10) * 3 * B) - 6;
  println("La expresión c) es: " + resultadoC);
}
